<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true">

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@color/background"
        android:orientation="vertical"
        android:padding="5dp" >

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingLeft="20sp"
            android:paddingRight="20sp">

            <Button
                android:id="@+id/button_currency_converter"
                style="@style/dashboard_button"
                android:drawableLeft="@drawable/currency_converter"
                android:onClick="clicked"
                android:text="@string/currency_converter"
                android:layout_marginRight="30sp"/>

            <Button
                android:id="@+id/button_transports"
                style="@style/dashboard_button"
                android:drawableLeft="@drawable/transports"
                android:onClick="clicked"
                android:text="@string/transports"
                android:layout_marginLeft="30sp"/>
        </LinearLayout>

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingLeft="20sp"
            android:paddingRight="20sp">

            <Button
                android:id="@+id/button_translator"
                style="@style/dashboard_button"
                android:drawableLeft="@drawable/translator"
                android:onClick="clicked"
                android:text="@string/translator"
                android:layout_marginRight="30sp"/>

            <Button
                android:id="@+id/button_phrasebook"
                style="@style/dashboard_button"
                android:drawableLeft="@drawable/phrasebook"
                android:onClick="clicked"
                android:text="@string/phrasebook"
                android:layout_marginLeft="30sp" />
        </LinearLayout>

    </LinearLayout>

</ScrollView>